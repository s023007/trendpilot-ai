TrendPilot AI — source visibility fix v2.2

Upload and replace these four files using the same paths:

1. index.html
2. trends.html
3. trend.html
4. js/app.js

Do not change:
- GitHub Actions workflow
- any Secret
- config/
- data/
- js/matched-products.js

Changes:
- Adds a homepage section showing the strongest published product from every advertiser.
- Joom and Geekbuying are no longer visually buried by many cards from a larger source.
- Product-first ranking inside every trend remains unchanged.
- Unknown prices no longer show as USD 0.00.
- Browser cache version is updated to v2.2.0.

After upload:
1. Open the website in a private tab or refresh it strongly.
2. No GitHub Action run is required for this visual-only update.
3. The new “Published source coverage” section shows one card for every advertiser
   that currently has at least one published verified offer.

A source with thousands of imported products will remain absent when none of its
products has passed trend matching, approval and exact-link validation. This is intentional.
