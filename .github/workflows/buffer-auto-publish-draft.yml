name: Buffer Auto Publish Draft

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Alignment notice
        run: |
          echo "Buffer draft automation is temporarily manual-only while TrendPilot is aligned to Pinterest, TikTok and YouTube only."
