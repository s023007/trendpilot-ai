name: Pinterest Search-First Growth Loop (Paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Pinterest publishing is intentionally paused
        run: |
          echo "Pinterest automatic publishing is paused while TrendPilot replaces the old static/video creatives with new AI-generated visual posts."
          echo "No Buffer post is created by this workflow."
