(() => {
  "use strict";
  const V="20.8.7",d=document,$=s=>d.querySelector(s),E=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
  const p=new URLSearchParams(location.search),id=p.get("id")||"";
  const money=r=>`${r.currency==="USD"?"US$":E((r.currency||"")+" ")}${Number(r.price||0).toLocaleString(undefined,{maximumFractionDigits:2})}`;
  function rarity(score){score=Number(score||0);if(score>=90)return`Exceptional find · ${score}`;if(score>=80)return`Very rare · ${score}`;if(score>=65)return`Hard to find · ${score}`;return`Specialist find · ${score}`}
  function priceBlock(r){if(!r.price)return`<p class="tp80-price">Check current price</p><p class="tp80-note">No exact current price is stored. Confirm it with the seller.</p>`;if(r.exact)return`<p class="tp80-price">${E(money(r))}</p><span class="tp80-price-proof verified">✓ Exact price evidence</span>`;return`<p class="tp80-price">${E(money(r))}</p><span class="tp80-price-proof feed">Seller-feed price</span><p class="tp80-note">This price came from the seller feed. Confirm the current price before payment.</p>`}
  function marketNotice(r){if(r.sellerSlug==="tiktok-shop-us")return`<section class="tp80-info"><h2>TikTok Shop US availability</h2><p>This listing comes from the US TikTok Shop market. Availability can differ by buyer location, shipping address, TikTok account eligibility, stock and whether the listing is still active. An error outside the US does not by itself prove that the product is unavailable to US buyers.</p></section>`;if(r.sellerSlug==="alibaba")return`<section class="tp80-info"><h2>Marketplace route</h2><p>Alibaba listings can sometimes open a broader marketplace route rather than a guaranteed exact-product destination. Confirm the exact item, seller and specifications before ordering.</p></section>`;return``}
  function render(r){const signals=(r.signals||[]).slice(0,5).map(s=>`<span>${E(s.replaceAll("-"," "))}</span>`).join("");const condition=E(r.condition||"Check seller");const sellerButton=r.url?`<a class="tp80-primary" href="${E(r.url)}" target="_blank" rel="sponsored nofollow noopener">Visit ${E(r.seller)} ↗</a>`:"<p class=\"tp80-note\">Seller destination is not currently available.</p>";$("[data-rare-detail]").innerHTML=`<section class="tp80-detail-hero"><div class="tp80-detail-media"><img src="${E(r.image)}" alt="${E(r.title)}" width="800" height="800"></div><div class="tp80-detail-copy"><span class="tp80-rare-score">${E(rarity(r.rareScore))}</span><p class="tp80-brand">${E(r.brand||r.seller)}</p><h1>${E(r.title)}</h1>${priceBlock(r)}<div class="tp80-signals">${signals}</div><div class="tp80-facts"><span><b>Seller</b>${E(r.seller)}</span><span><b>Product type</b>${E(r.typeLabel||r.type)}</span><span><b>Condition</b>${condition}</span></div><p class="tp80-note">You are still on TrendPilot. The seller opens only when you use the button below.</p>${sellerButton}</div></section>${marketNotice(r)}<section class="tp80-info"><h2>Before you buy</h2><p>Confirm the exact model, condition, current price, stock, shipping destination and return terms with the seller. Rarity is a discovery signal, not a guarantee of value.</p><a href="/find/?q=${encodeURIComponent(r.title)}&universal=1">Search for similar products</a></section>`;d.title=`${r.title} — Rare Find | TrendPilot AI`}
  async function boot(){const host=$("[data-rare-detail]");if(!id){host.innerHTML='<section class="tp80-info"><h1>Product not specified</h1><p>Return to Rare Finds and choose a product.</p><a href="/rare-used/">Back to Rare Finds</a></section>';return}try{const res=await fetch(`/data/v20-8/rare-index.json?v=${V}`,{cache:"no-cache"});if(!res.ok)throw new Error("rare index unavailable");const rows=await res.json();const row=rows.find(x=>String(x.id)===id);if(!row)throw new Error("product not found");render(row)}catch(e){host.innerHTML='<section class="tp80-info"><h1>Product details are updating</h1><p>This item could not be loaded right now. Please return to Rare Finds and try again.</p><a href="/rare-used/">Back to Rare Finds</a></section>'}}
  d.readyState==="loading"?d.addEventListener("DOMContentLoaded",boot,{once:true}):boot();
})();